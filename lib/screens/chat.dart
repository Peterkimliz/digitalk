import 'package:flutter/material.dart';

class Chat extends StatelessWidget {
  Chat({Key? key, required channelId}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
